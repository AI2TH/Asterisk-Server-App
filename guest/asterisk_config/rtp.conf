; rtp.conf — RTP media port range
; Must match the SLIRP hostfwd UDP ports in VmManager (10000-10019).
; 20 ports = up to 10 concurrent calls (2 RTP ports per call).

[general]
rtpstart=10000
rtpend=10019
