; manager.conf — Asterisk Manager Interface (AMI)
; Bound to 127.0.0.1 inside the VM; accessible from Android via SLIRP hostfwd tcp::5038-:5038

[general]
enabled=yes
port=5038
bindaddr=127.0.0.1
timestampevents=yes

[stardial]
secret=stardial_ami_secret
read=all
write=all
